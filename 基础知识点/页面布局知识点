1.多个div盒子本来应该纵向排列，给父盒子加了display：flex  变成横向排列

2.父盒子flex-wrap默认nowrap 不换行  类 white-space: nowrap 不换行溢出显示

（1）在同一行/列显示   

（2）放不下时会通过自动压缩宽度/高度来自适应，宽/高被压缩到无法再压缩时溢出

flex-wrap: wrap：只有当父元素的宽度/高度小于子元素时才会发生换行，否则即使设置了换行也会在同一行/列显示
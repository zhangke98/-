push() 方法可向数组的末尾添加一个或多个元素，并返回新的长度。还可以添加数组
find()方法，返回通过测试(函数内判断)的数组的第一个元素的值它的参数是一个回调函数，所有数组成员依次执行该回调函数，直到找出第一个返回值为true的成员，然后返回该成员。如果没有符合条件的成员，则返回undefined。

splice返回被删除的项目长度而不是数组所以后面不能再跟方法

array.concat(arrayX,arrayY)

slice()只传一个参数的时候是返回的从起始位置元素到最后一个元素
indexOf() 方法可返回某个指定的字符串值在字符串中首次出现的位置。而不是1


split方法:
var str= 'D:\\Web-workspace\\知识点题\\public\\a\\b\\c'
var res=str.split('public')
console.log(res);

array(2)
0: "D:\Web-workspace\知识点题\"
1: "\a\b\c"
length: 2
 
public没了以什么为分割就是那什么没了




sort方法：
 a-b 升序, b-a 降序
 毕昇 爱酱
 如果调用该方法时没有使用参数，将按字母顺序对数组中的元素进行排序，说得更精确点，是按照字符编码的顺序进行排序


lastMsgsArr.sort(function (m1, m2) {   
    // return m2-m1 不可以 
    return m2.create_time-m1.create_time
})
按时间降序
平时需要的都是数字



join方法：
["e", "e", "e", "e", "2"]  join(',')或者join()   结果e,e,e,e,2  会默认有逗号
.join('')才是eeee2


replace是返回新的字符串


js中会改变原数组的   pop  push  shift  unshift  sort splice  还有reverse！！！！



fromCharCode() 可接受一个指定的 Unicode 值，然后返回一个字符串。
该方法是 String 的静态方法，使用语法： String.fromCharCode()。
var n = String.fromCharCode(65);
n 输出结果:A
.charCodeAt() 方法可返回指定位置的字符的 Unicode 编码。
str.charCodeAt(1)